version: '3.4'
services:
  web:
    build:
      context: .
    domainname: docker
    container_name: bolt
    hostname: bolt
    network_mode: bridge
    ports:
      - target: 80
        published: 80
        protocol: tcp
        mode: host
